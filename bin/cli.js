#!/usr/bin/env node
console.error(
  "aglaya-tokens-version: this npm name is a placeholder.\n" +
  "The real tool ships with @aglaya/design-tokens. Install it and run:\n" +
  '  npm install "git+https://github.com/ibaifernandez/aglaya-design-system.git#v1.4.0"\n' +
  "  npm exec --no -- aglaya-tokens-version"
);
process.exit(1);
